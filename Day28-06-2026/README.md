# MERN Stack Training - Daily Diary

## Day 4

**Date:** 28-06-2026

### Topics Covered
- Introduction to CSS
- Types of CSS
  - Inline CSS
  - Internal CSS
  - External CSS
- CSS Selectors
  - Element Selector
  - Class Selector
  - ID Selector
- Basic CSS Properties
  - Height
  - Width
  - Background Color
  - Border Radius
- Text Styling in CSS
- Background Properties
- Linking an External CSS File

### Practical Work
- Created a webpage and linked an external CSS file.
- Styled `<div>` elements using class (`.a`) and ID (`#ab`) selectors.
- Applied different background colors using hexadecimal color codes.
- Used the `border-radius` property to create rounded corners.
- Styled text using CSS properties such as:
  - `color`
  - `font-size`
  - `font-weight`
  - `text-decoration`
  - `text-align`
  - `text-shadow`
  - `font-family`
- Added a background image to a `<div>` element.
- Used background properties including:
  - `background-image`
  - `background-repeat`
  - `background-size`
  - `background-position`
  - `background-attachment`
- Observed the effect of a fixed background while scrolling.

### Concepts Learned
- Difference between inline, internal, and external CSS.
- Using class and ID selectors for styling HTML elements.
- Applying text formatting and typography using CSS.
- Working with background images and controlling their display.
- Understanding the use of `cover`, `center`, `no-repeat`, and `fixed` background properties.
- Understanding CSS cascading and style precedence.

### Outcome
Successfully learned the fundamentals of CSS styling, including selectors, text formatting, colors, border radius, and background properties. Practiced creating visually appealing web pages using internal and external CSS.

---

**Author:** Ashmeen Kaur  
**Training:** Web Development Training  
**URN:** 2302486
